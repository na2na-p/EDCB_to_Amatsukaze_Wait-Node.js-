import {describe, test, expect} from 'vitest';
import {Send} from '../src/class/Send.js';

describe('Send', () => {
	test('Expected behavior', () => (
		// @ts-ignore
		expect(() => new Send())
	));
});
